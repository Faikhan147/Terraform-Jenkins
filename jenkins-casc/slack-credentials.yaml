credentials:
  system:
    domainCredentials:
      - domain: global
        credentials:
          - string:
              scope: GLOBAL
              id: "slack-webhook"
              secret: "${SLACK_SECRET}"
              description: "Slack Token for Notifications"
